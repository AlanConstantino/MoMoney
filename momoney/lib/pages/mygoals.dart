import 'package:flutter/material.dart';

class MyGoals extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("This is a test -- goals"),
    );
  }
}
